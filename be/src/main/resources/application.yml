spring:
  application:
    name: backend
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:vst}
    username: ${DB_USER:vst}
    password: ${DB_PASSWORD:vst}
  flyway:
    enabled: true
    locations: classpath:db/migration

management:
  endpoints:
    web:
      exposure:
        include: health,info

vnstock:
  base-url: ${VNSTOCK_SERVICE_URL:http://localhost:8000}

ingestion:
  symbols: ${INGESTION_SYMBOLS:VNM,VCB,FPT}
  lookback-days: ${INGESTION_LOOKBACK_DAYS:7}
  cron: ${INGESTION_CRON:0 0 18 * * MON-FRI}

resilience4j:
  retry:
    instances:
      vnstock:
        max-attempts: 3
        wait-duration: 500ms
  circuitbreaker:
    instances:
      vnstock:
        sliding-window-size: 10
        failure-rate-threshold: 50
        wait-duration-in-open-state: 30s
