# Copy to .env and fill in real values. Never commit .env.

# --- Backend (Spring Boot) ---
SPRING_PROFILES_ACTIVE=dev

# --- Database (added in Phase 0 Step 2 - Docker + DB) ---
# POSTGRES_DB=vst
# POSTGRES_USER=vst
# POSTGRES_PASSWORD=changeme
